export {};
// export const drawCircle = (context: CanvasRenderingContext2D, a: Point, radius: number, fillStyle: string = `red`) => {
//   context.fillStyle = fillStyle;
//   context.beginPath();
//   context.arc(a.x, a.y, radius, 0, Math.PI * 2);
//   context.closePath();
//   context.fill();
// }
// export type Drawing = {
//   circle: (a: Point, radius: number, fillStyle: string) => void
// }
// export const drawing = (context: CanvasRenderingContext2D): Drawing => ({
//   circle: (a: Point, radius: number, fillStyle: string) => drawCircle(context, a, radius, fillStyle)
// })
//# sourceMappingURL=drawing-util.js.map